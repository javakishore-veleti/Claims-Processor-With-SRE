# AWS-specific configuration
# Datasource, Redis injected via SPRING_DATASOURCE_* and SPRING_DATA_REDIS_* env vars
# set by ECS task definition from Secrets Manager — no explicit overrides needed.
# Compose with environment profile: --spring.profiles.active=dev,aws
spring:
  datasource:
    driver-class-name: org.postgresql.Driver

# Override dev feature toggles for AWS (disable services not deployed)
entitlements:
  features:
    search:
      enabled: ${FEATURE_SEARCH_ENABLED:false}
    event-stream:
      enabled: ${FEATURE_EVENT_STREAM_ENABLED:false}
    log-shipping:
      enabled: ${FEATURE_LOG_SHIPPING_ENABLED:false}
    cache:
      enabled: ${FEATURE_CACHE_ENABLED:false}
    metrics:
      enabled: true

# AWS-specific properties
aws:
  region: ${AWS_REGION:us-east-1}
  s3:
    bucket: ${AWS_S3_BUCKET:}
  secrets-manager:
    enabled: true
  cloudwatch:
    enabled: true

management:
  health:
    redis:
      enabled: ${FEATURE_CACHE_ENABLED:false}
    kafka:
      enabled: ${FEATURE_EVENT_STREAM_ENABLED:false}
    elasticsearch:
      enabled: ${FEATURE_SEARCH_ENABLED:false}
