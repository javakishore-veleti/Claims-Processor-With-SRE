# Local development profile - H2 in-memory database, debug logging
# Activate with: --spring.profiles.active=local
spring:
  h2:
    console:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
  thymeleaf:
    cache: false
  data:
    redis:
      host: localhost
      port: 6379

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always

logging:
  level:
    root: INFO
    com.healthcare.claims.portal.member: DEBUG
    org.springframework.security: DEBUG
    org.springframework.web: DEBUG
